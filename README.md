# Multi-Platform Matrix Build with Artifacts

This repository demonstrates a GitHub Actions matrix build that runs parallel jobs
for multiple Node.js versions and uploads unique build artifacts for each.

Each artifact is named `build-1669581-node<version>`.

**Email:** 22f1001439@ds.study.iitm.ac.in
